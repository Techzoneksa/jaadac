"use client";import { PageHeader } from "@/components/ui/PageHeader";import { DataTable, type Column } from "@/components/ui/DataTable";import { Button } from "@/components/ui/button";import { MoneyDisplay } from "@/components/ui/MoneyDisplay";import { StatusBadge } from "@/components/ui/StatusBadge";import { useApi } from "@/lib/hooks/use-api";import { useRouter } from "next/navigation";import Link from "next/link";interface DNRec { id: string; number: string; date: string; supplier_name: string; total: number; status: string;}const columns: Column<DNRec>[] = [  { key: "number", header: "الرقم" },  { key: "date", header: "التاريخ" },  { key: "supplier_name", header: "المورد" },  { key: "status", header: "الحالة", render: (r) => <StatusBadge status={r.status} /> },  { key: "total", header: "الإجمالي", render: (r) => <MoneyDisplay amount={r.total} /> },];export default function DebitNotesPage() {  const { data, loading, error } = useApi<DNRec>("/api/debit-notes");  const router = useRouter();  return (    <div>      <PageHeader title="إشعارات مدينة"        action={<Link href="/purchases/debit-notes/new"><Button>إشعار مدين جديد</Button></Link>} />      <DataTable columns={columns} data={data} keyExtractor={(r) => r.id}        isLoading={loading} error={error}        emptyTitle="لا توجد إشعارات مدينة" />    </div>  );}
