"use client";export default function ErrorPage({ reset }: { error: Error; reset: () => void }) {  return (    <div className="flex min-h-screen flex-col items-center justify-center gap-4">      <h1 className="text-4xl font-bold text-danger">خطأ</h1>      <p className="text-muted">حدث خطأ غير متوقع</p>      <button onClick={() => reset()} className="rounded bg-primary px-4 py-2 text-white">        إعادة المحاولة      </button>    </div>  );}