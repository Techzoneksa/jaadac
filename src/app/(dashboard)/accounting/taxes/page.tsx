"use client";import { PageHeader } from "@/components/ui/PageHeader";import { DataTable, type Column } from "@/components/ui/DataTable";import { useApi } from "@/lib/hooks/use-api";import type { TaxRate } from "@/lib/types";const columns: Column<TaxRate>[] = [  { key: "name_ar", header: "الاسم" },  { key: "rate", header: "النسبة", render: (r) => `${r.rate}%` },  { key: "tax_type", header: "النوع" },  {    key: "is_active",    header: "الحالة",    render: (r) => (r.is_active ? "نشط" : "غير نشط"),  },];export default function TaxesPage() {  const { data, loading } = useApi<TaxRate>("/api/tax-rates");  return (    <div>      <PageHeader title="الضرائب" description="إعدادات الضرائب" />      <DataTable        columns={columns}        data={data}        keyExtractor={(r) => r.id}        isLoading={loading}        emptyTitle="لا توجد ضرائب"      />    </div>  );}
