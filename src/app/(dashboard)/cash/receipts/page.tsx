"use client";import { PageHeader } from "@/components/ui/PageHeader";import { DataTable, type Column } from "@/components/ui/DataTable";import { Button } from "@/components/ui/button";import { MoneyDisplay } from "@/components/ui/MoneyDisplay";import { StatusBadge } from "@/components/ui/StatusBadge";import { useApi } from "@/lib/hooks/use-api";import { useRouter } from "next/navigation";import Link from "next/link";interface Receipt {  id: string;  number: string;  date: string;  customer_name: string;  amount: number;  status: string;}const columns: Column<Receipt>[] = [  { key: "number", header: "الرقم" },  { key: "date", header: "التاريخ" },  { key: "customer_name", header: "العميل" },  { key: "status", header: "الحالة", render: (r) => <StatusBadge status={r.status} /> },  { key: "amount", header: "المبلغ", render: (r) => <MoneyDisplay amount={r.amount} /> },];export default function ReceiptsPage() {  const { data, loading } = useApi<Receipt>("/api/receipts");  const router = useRouter();  return (    <div>      <PageHeader        title="سندات القبض"        action={          <Link href="/cash/receipts/new">            <Button>سند قبض جديد</Button>          </Link>        }      />      <DataTable        columns={columns}        data={data}        keyExtractor={(r) => r.id}        isLoading={loading}        onRowClick={(r) => router.push(`/cash/receipts/${r.id}`)}        emptyTitle="لا توجد سندات قبض"      />    </div>  );}
