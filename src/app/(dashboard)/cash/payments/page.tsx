"use client";import { PageHeader } from "@/components/ui/PageHeader";import { DataTable, type Column } from "@/components/ui/DataTable";import { Button } from "@/components/ui/button";import { MoneyDisplay } from "@/components/ui/MoneyDisplay";import { StatusBadge } from "@/components/ui/StatusBadge";import { useApi } from "@/lib/hooks/use-api";import { useRouter } from "next/navigation";import Link from "next/link";interface Payment {  id: string;  number: string;  date: string;  supplier_name: string;  amount: number;  status: string;}const columns: Column<Payment>[] = [  { key: "number", header: "الرقم" },  { key: "date", header: "التاريخ" },  { key: "supplier_name", header: "المورد" },  { key: "status", header: "الحالة", render: (r) => <StatusBadge status={r.status} /> },  { key: "amount", header: "المبلغ", render: (r) => <MoneyDisplay amount={r.amount} /> },];export default function PaymentsPage() {  const { data, loading } = useApi<Payment>("/api/payments");  const router = useRouter();  return (    <div>      <PageHeader        title="سندات الصرف"        action={          <Link href="/cash/payments/new">            <Button>سند صرف جديد</Button>          </Link>        }      />      <DataTable        columns={columns}        data={data}        keyExtractor={(r) => r.id}        isLoading={loading}        onRowClick={(r) => router.push(`/cash/payments/${r.id}`)}        emptyTitle="لا توجد سندات صرف"      />    </div>  );}
