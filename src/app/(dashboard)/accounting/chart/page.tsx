"use client";import { PageHeader } from "@/components/ui/PageHeader";import { DataTable, type Column } from "@/components/ui/DataTable";interface Account {  code: string;  name_ar: string;  type: string;  balance: number;}const columns: Column<Account>[] = [  { key: "code", header: "الرمز" },  { key: "name_ar", header: "اسم الحساب" },  { key: "type", header: "النوع" },  {    key: "balance",    header: "الرصيد",    render: (r) => `${r.balance.toLocaleString()} ر.س`,  },];export default function ChartOfAccountsPage() {  const data: Account[] = [];  return (    <div>      <PageHeader title="شجرة الحسابات" description="دليل الحسابات المحاسبي" />      <DataTable        columns={columns}        data={data}        keyExtractor={(r) => r.code}        emptyTitle="لا توجد حسابات"        emptyDescription="قم بإضافة الحسابات المحاسبية"      />    </div>  );}