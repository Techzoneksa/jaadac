import { useState } from "react";import { QuickCreateDialog } from "./QuickCreateDialog";import { Input } from "@/components/ui/input";import { Label } from "@/components/ui/label";import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";import { useI18n } from "@/lib/i18n";import { ItemService } from "@/lib/services";import { newId, type Item } from "@/lib/store";import { useAudit } from "@/hooks/useAudit";import { toast } from "sonner";export function QuickCreateItemDialog({  open, onOpenChange, onCreated,}: { open: boolean; onOpenChange: (v: boolean) => void; onCreated?: (i: Item) => void }) {  const { lang } = useI18n();  const ar = lang === "ar";  const audit = useAudit();  const empty: Item = {    id: newId(), name_ar: "", name_en: "", type: "service",    sales_price: 0, taxable: true, vat_rate: 15, status: "active",  };  const [form, setForm] = useState<Item>(empty);  const [addAnother, setAddAnother] = useState(false);  const reset = () => setForm({ ...empty, id: newId() });  const handleSave = () => {    if (!form.name_ar.trim() && !form.name_en.trim()) {      toast.error(ar ? "الاسم مطلوب" : "Name is required");      return false;    }    if (form.sales_price < 0) {      toast.error(ar ? "السعر لا يمكن أن يكون سالبًا" : "Price cannot be negative");      return false;    }    ItemService.create(form);    audit.log("item.created", "item", `إضافة منتج ${form.name_ar || form.name_en}`, `Created item ${form.name_en || form.name_ar}`, form.id);    toast.success(ar ? "تم إنشاء المنتج" : "Item created");    onCreated?.(form);    reset();    return true;  };  return (    <QuickCreateDialog      open={open}      onOpenChange={(v) => { if (!v) reset(); onOpenChange(v); }}      titleAr="إضافة منتج/خدمة سريع"      titleEn="Quick add product / service"      onSave={handleSave}      showSaveAndAddAnother      saveAndAddAnother={addAnother}      onSaveAndAddAnotherChange={setAddAnother}      size="md"    >      <div className="grid grid-cols-2 gap-3">        <div className="space-y-1.5">          <Label>{ar ? "الاسم (عربي)" : "Name (AR)"}</Label>          <Input value={form.name_ar} onChange={(e) => setForm({ ...form, name_ar: e.target.value })} />        </div>        <div className="space-y-1.5">          <Label>{ar ? "الاسم (إنجليزي)" : "Name (EN)"}</Label>          <Input value={form.name_en} onChange={(e) => setForm({ ...form, name_en: e.target.value })} />        </div>        <div className="space-y-1.5">          <Label>{ar ? "النوع" : "Type"}</Label>          <Select value={form.type} onValueChange={(v) => setForm({ ...form, type: v as Item["type"] })}>            <SelectTrigger><SelectValue /></SelectTrigger>            <SelectContent>              <SelectItem value="service">{ar ? "خدمة" : "Service"}</SelectItem>              <SelectItem value="non_stock">{ar ? "غير مخزني" : "Non-stock"}</SelectItem>              <SelectItem value="stock">{ar ? "مخزني" : "Stock"}</SelectItem>            </SelectContent>          </Select>        </div>        <div className="space-y-1.5">          <Label>SKU</Label>          <Input value={form.sku || ""} onChange={(e) => setForm({ ...form, sku: e.target.value })} />        </div>        <div className="space-y-1.5">          <Label>{ar ? "سعر البيع" : "Sales price"}</Label>          <Input type="number" value={form.sales_price} onChange={(e) => setForm({ ...form, sales_price: +e.target.value })} />        </div>        <div className="space-y-1.5">          <Label>{ar ? "نسبة الضريبة %" : "VAT %"}</Label>          <Input type="number" value={form.vat_rate} onChange={(e) => setForm({ ...form, vat_rate: +e.target.value })} />        </div>      </div>    </QuickCreateDialog>  );}