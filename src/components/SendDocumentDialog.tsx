import { useState, type ReactNode } from "react";import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger } from "@/components/ui/dialog";import { Input } from "@/components/ui/input";import { Textarea } from "@/components/ui/textarea";import { Button } from "@/components/ui/button";import { Mail, MessageSquare, Copy, AlertTriangle } from "lucide-react";import { useI18n } from "@/lib/i18n";import { toast } from "sonner";/** * Phase 2.2 — Send via Email / WhatsApp placeholder. * * Strictly demo UI: no APIs are called, no credentials are read. */export function SendDocumentDialog({  trigger,  channel,  documentLabel,  defaultRecipient,  defaultSubject,  defaultMessage,}: {  trigger: ReactNode;  channel: "email" | "whatsapp";  documentLabel: string;  defaultRecipient?: string;  defaultSubject?: string;  defaultMessage?: string;}) {  const { lang } = useI18n();  const [open, setOpen] = useState(false);  const [recipient, setRecipient] = useState(defaultRecipient || "");  const [subject, setSubject] = useState(defaultSubject || "");  const [message, setMessage] = useState(defaultMessage || "");  const title =    channel === "email"      ? (lang === "ar" ? "إرسال عبر البريد" : "Send via Email")      : (lang === "ar" ? "إرسال عبر واتساب" : "Send via WhatsApp");  const Icon = channel === "email" ? Mail : MessageSquare;  return (    <Dialog open={open} onOpenChange={setOpen}>      <DialogTrigger asChild>{trigger}</DialogTrigger>      <DialogContent className="max-w-lg">        <DialogHeader>          <DialogTitle className="flex items-center gap-2">            <Icon className="size-4" />            {title}          </DialogTitle>        </DialogHeader>        <div className="space-y-3">          <div className="text-xs text-muted-foreground">{documentLabel}</div>          <div>            <label className="text-xs block mb-1">              {channel === "email"                ? (lang === "ar" ? "البريد" : "Email")                : (lang === "ar" ? "رقم الجوال" : "Mobile")}            </label>            <Input value={recipient} onChange={(e) => setRecipient(e.target.value)} />          </div>          {channel === "email" && (            <div>              <label className="text-xs block mb-1">{lang === "ar" ? "الموضوع" : "Subject"}</label>              <Input value={subject} onChange={(e) => setSubject(e.target.value)} />            </div>          )}          <div>            <label className="text-xs block mb-1">{lang === "ar" ? "الرسالة" : "Message"}</label>            <Textarea rows={4} value={message} onChange={(e) => setMessage(e.target.value)} />          </div>          <div className="rounded-md border border-warning/30 bg-warning/10 p-3 text-xs flex gap-2">            <AlertTriangle className="size-4 text-warning shrink-0" />            <span>              {lang === "ar"                ? "الإرسال غير مفعّل بعد. هذه نافذة معاينة للتجهيز فقط."                : "Sending is not connected yet. This is a preview dialog only."}            </span>          </div>          <div className="flex justify-between gap-2">            <Button              variant="outline"              size="sm"              onClick={() => {                navigator.clipboard?.writeText(`${documentLabel}\n${message}`);                toast.success(lang === "ar" ? "تم النسخ" : "Copied");              }}            >              <Copy className="size-4 me-1" />              {lang === "ar" ? "نسخ رابط عام" : "Copy public link"}            </Button>            <Button              size="sm"              onClick={() => {                toast.info(lang === "ar" ? "الإرسال غير مفعّل بعد" : "Sending is not connected yet");                setOpen(false);              }}            >              {title}            </Button>          </div>        </div>      </DialogContent>    </Dialog>  );}