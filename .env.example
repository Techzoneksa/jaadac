# JAAD CLOUD — environment example
# Demo mode does not require any of these. Backend mode (when enabled later) reads them.

# Client-visible (browser):
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-only (NEVER commit real values):
SUPABASE_SERVICE_ROLE_KEY=server-only-placeholder
SUPABASE_JWT_SECRET=server-only-placeholder
