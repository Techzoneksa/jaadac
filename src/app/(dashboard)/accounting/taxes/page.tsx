"use client";import { PageHeader } from "@/components/ui/PageHeader";import { DataTable, type Column } from "@/components/ui/DataTable";interface Tax {  name: string;  rate: number;}const columns: Column<Tax>[] = [  { key: "name", header: "الاسم" },  { key: "rate", header: "النسبة", render: (r) => `${r.rate}%` },];export default function TaxesPage() {  const data: Tax[] = [{ name: "ضريبة القيمة المضافة", rate: 15 }];  return (    <div>      <PageHeader title="الضرائب" description="إعدادات الضرائب" />      <DataTable columns={columns} data={data} keyExtractor={(r) => r.name} />    </div>  );}