# JAAD CLOUD — Hostinger Readiness

> **Status:** Hostinger root directory locked to `./`. Root `npm run build` proxies to `apps/web`.

## Hostinger Settings (Required)

| Setting | Value |
|---------|-------|
| Branch | `nextjs-migration` |
| Root directory | `./` |
| Framework | Next.js |
| Build command | `npm run build` |
| Output directory | `.next` |
| Node version | 22.x |
| Package manager | npm |

## How Build Works

1. Hostinger runs `npm install` in `./` (installs old demo deps from root `package.json`)
2. Hostinger runs `npm run build` (root) → executes `node scripts/hostinger-build.mjs`
3. The script:
   - Runs `npm install` inside `apps/web` (Next.js dependencies)
   - Runs `npm run build` inside `apps/web` (Next.js production build)
   - Copies `apps/web/.next` → `./.next` (Hostinger detects output at root level)
4. Hostinger runs `npm start` → `npm --prefix apps/web run start` (Next.js server from `apps/web`)

## Root Scripts

| Script | Purpose |
|--------|---------|
| `npm run build` | **Hostinger build** — builds Next.js + copies `.next` to root |
| `npm start` | **Hostinger start** — starts Next.js from `apps/web` |
| `npm run dev:web` | Local Next.js dev server |
| `npm run build:web` | Local Next.js build only (no root copy) |
| `npm run demo:build` | Old demo Vite build (unchanged) |
| `npm run dev` | Old demo Vite dev server (unchanged) |

## Root `.next` Handling

- Root `.next/` is listed in `.gitignore` (line 30) — never committed
- Generated by `scripts/hostinger-build.mjs` at deploy time
- Cleaned up before each copy (no stale artifacts)

## Environment Variables (Set in Hostinger Dashboard)

| Variable | Purpose | Status |
|----------|---------|--------|
| `DATABASE_URL` | PostgreSQL connection | ❌ Phase 2.3+ |
| `DIRECT_URL` | Direct DB connection (migrations) | ❌ Phase 2.3+ |
| `JWT_SECRET` | Auth token secret | ❌ Phase 2.4 |
| `AUTH_SECRET` | Auth.js encryption | ❌ Phase 2.4 |
| `AUTH_URL` | Public app URL | ❌ Phase 2.4 |
| `NEXT_PUBLIC_APP_URL` | Client-side app URL | ❌ Phase 2.4 |
| `NODE_ENV` | Environment (production/staging) | ❌ Manual |

## Deployment Checklist

- [x] Branch set to `nextjs-migration`
- [x] Root directory set to `./`
- [x] Framework set to Next.js
- [x] Build output directory set to `.next`
- [x] Node version set to 22.x
- [ ] Environment variables configured
- [ ] Database provisioned
- [ ] Auth configured
- [ ] SSL enabled (auto via Let's Encrypt)

## Known Limitations

- First build may be slower (installs deps in `apps/web`)
- Two `package-lock.json` files exist (root + `apps/web`) — handled via `outputFileTracingRoot`
- Old demo app not deployable from `phase-1-migration-audit` to Hostinger (different framework)
