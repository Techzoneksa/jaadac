import type { ReactNode } from "react";import { useAuth, type Permission } from "@/lib/auth";import { AccessDenied } from "@/components/AccessDenied";/** * Wrap any UI that requires a specific permission. *  - mode="page"  → render <AccessDenied /> as a full page fallback *  - mode="hide"  → render nothing (default) — use for action buttons *  - mode="fallback" → render `fallback` prop */export function PermissionGate({  perm, perms, mode = "hide", children, fallback,}: {  perm?: Permission;  perms?: Permission[];  mode?: "page" | "hide" | "fallback";  children: ReactNode;  fallback?: ReactNode;}) {  const { can, canAny } = useAuth();  const allowed = perm ? can(perm) : perms ? canAny(perms) : true;  if (allowed) return <>{children}</>;  if (mode === "page") return <AccessDenied />;  if (mode === "fallback") return <>{fallback ?? null}</>;  return null;}