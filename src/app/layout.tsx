import type { Metadata } from "next";import { Cairo, Noto_Sans_Arabic } from "next/font/google";import "./globals.css";const cairo = Cairo({  subsets: ["arabic"],  variable: "--font-cairo",  display: "swap",});const notoSans = Noto_Sans_Arabic({  subsets: ["arabic"],  variable: "--font-noto-sans",  display: "swap",});export const metadata: Metadata = {  title: "جاد كلاود | Jaad Cloud",  description: "نظام المحاسبة السحابي",};export default function RootLayout({ children }: { children: React.ReactNode }) {  return (    <html lang="ar" dir="rtl" className={`${cairo.variable} ${notoSans.variable}`}>      <body className="min-h-screen font-sans antialiased">{children}</body>    </html>  );}