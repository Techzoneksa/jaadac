"use client";import { PageHeader } from "@/components/ui/PageHeader";import { DataTable, type Column } from "@/components/ui/DataTable";interface JournalEntry {  id: string;  date: string;  description: string;  debit: number;  credit: number;}const columns: Column<JournalEntry>[] = [  { key: "date", header: "التاريخ" },  { key: "description", header: "البيان" },  { key: "debit", header: "مدين", render: (r) => `${r.debit.toLocaleString()} ر.س` },  { key: "credit", header: "دائن", render: (r) => `${r.credit.toLocaleString()} ر.س` },];export default function JournalPage() {  const data: JournalEntry[] = [];  return (    <div>      <PageHeader title="قيود اليومية" description="تسجيل القيود المحاسبية" />      <DataTable        columns={columns}        data={data}        keyExtractor={(r) => r.id}        emptyTitle="لا توجد قيود"      />    </div>  );}