"use client";import { PageHeader } from "@/components/ui/PageHeader";import { DataTable, type Column } from "@/components/ui/DataTable";import { Button } from "@/components/ui/button";import { MoneyDisplay } from "@/components/ui/MoneyDisplay";import { StatusBadge } from "@/components/ui/StatusBadge";import { useApi } from "@/lib/hooks/use-api";import { useRouter } from "next/navigation";import Link from "next/link";interface PORec { id: string; number: string; date: string; supplier_name: string; total: number; status: string;}const columns: Column<PORec>[] = [  { key: "number", header: "الرقم" },  { key: "date", header: "التاريخ" },  { key: "supplier_name", header: "المورد" },  { key: "status", header: "الحالة", render: (r) => <StatusBadge status={r.status} /> },  { key: "total", header: "الإجمالي", render: (r) => <MoneyDisplay amount={r.total} /> },];export default function PurchaseOrdersPage() {  const { data, loading, error } = useApi<PORec>("/api/purchase-orders");  const router = useRouter();  return (    <div>      <PageHeader title="أوامر الشراء"        action={<Link href="/purchases/orders/new"><Button>أمر شراء جديد</Button></Link>} />      <DataTable columns={columns} data={data} keyExtractor={(r) => r.id}        isLoading={loading} error={error}        onRowClick={(r) => router.push(`/purchases/orders/${r.id}`)}        emptyTitle="لا توجد أوامر شراء" />    </div>  );}
