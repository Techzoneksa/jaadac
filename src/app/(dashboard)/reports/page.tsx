import { PageHeader } from "@/components/ui/PageHeader";import { Card } from "@/components/ui/card";export default function ReportsPage() {  return (    <div>      <PageHeader title="مركز التقارير" description="التقارير المالية والإدارية" />      <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">        {["كشف الحساب", "دفتر الأستاذ", "ميزان المراجعة", "قائمة الدخل", "الميزانية العمومية", "تقارير المبيعات"].map((r) => (          <Card key={r} className="p-4 cursor-pointer hover:shadow-md transition-shadow">            <p className="font-medium">{r}</p>          </Card>        ))}      </div>    </div>  );}