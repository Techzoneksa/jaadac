"use client";import { PageHeader } from "@/components/ui/PageHeader";import { DataTable, type Column } from "@/components/ui/DataTable";import { useApi } from "@/lib/hooks/use-api";import type { JournalEntry } from "@/lib/types";const columns: Column<JournalEntry>[] = [  { key: "number", header: "الرقم" },  { key: "date", header: "التاريخ" },  { key: "description", header: "البيان" },  {    key: "status",    header: "الحالة",    render: (r) => (r.status === "posted" ? "مرحّل" : "مسودة"),  },];export default function JournalPage() {  const { data, loading } = useApi<JournalEntry>("/api/journal");  return (    <div>      <PageHeader title="قيود اليومية" description="تسجيل القيود المحاسبية" />      <DataTable        columns={columns}        data={data}        keyExtractor={(r) => r.id}        isLoading={loading}        emptyTitle="لا توجد قيود"        emptyDescription="أضف قيداً جديداً"      />    </div>  );}
