"use client";import { PageHeader } from "@/components/ui/PageHeader";import { DataTable, type Column } from "@/components/ui/DataTable";import { useApi } from "@/lib/hooks/use-api";import type { Account } from "@/lib/types";const columns: Column<Account>[] = [  { key: "number", header: "الرمز" },  { key: "name_ar", header: "اسم الحساب" },  { key: "type", header: "النوع" },  {    key: "status",    header: "الحالة",    render: (r) => (r.status === "active" ? "نشط" : "غير نشط"),  },];export default function ChartOfAccountsPage() {  const { data, loading } = useApi<Account>("/api/accounts");  return (    <div>      <PageHeader title="شجرة الحسابات" description="دليل الحسابات المحاسبي" />      <DataTable        columns={columns}        data={data}        keyExtractor={(r) => r.id}        isLoading={loading}        emptyTitle="لا توجد حسابات"        emptyDescription="قم بإضافة الحسابات المحاسبية"      />    </div>  );}
