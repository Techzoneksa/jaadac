"use client";import { PageHeader } from "@/components/ui/PageHeader";import { DataTable, type Column } from "@/components/ui/DataTable";import { Button } from "@/components/ui/button";import { MoneyDisplay } from "@/components/ui/MoneyDisplay";import { StatusBadge } from "@/components/ui/StatusBadge";import { useApi } from "@/lib/hooks/use-api";import { useRouter } from "next/navigation";import Link from "next/link";interface CNRec { id: string; number: string; date: string; customer_name: string; total: number; status: string;}const columns: Column<CNRec>[] = [  { key: "number", header: "الرقم" },  { key: "date", header: "التاريخ" },  { key: "customer_name", header: "العميل" },  { key: "status", header: "الحالة", render: (r) => <StatusBadge status={r.status} /> },  { key: "total", header: "الإجمالي", render: (r) => <MoneyDisplay amount={r.total} /> },];export default function CreditNotesPage() {  const { data, loading, error } = useApi<CNRec>("/api/credit-notes");  const router = useRouter();  return (    <div>      <PageHeader title="إشعارات دائنة"        action={<Link href="/sales/credit-notes/new"><Button>إشعار دائن جديد</Button></Link>} />      <DataTable columns={columns} data={data} keyExtractor={(r) => r.id}        isLoading={loading} error={error}        emptyTitle="لا توجد إشعارات دائنة" />    </div>  );}
