"use client";import { useEffect, useState } from "react";import { useRouter, useParams } from "next/navigation";import { PageHeader } from "@/components/ui/PageHeader";import { Button } from "@/components/ui/button";import { Label } from "@/components/ui/label";import { Card, CardContent } from "@/components/ui/card";type PODetail = Record<string, unknown> & {  number?: string; date?: string; status?: string; subtotal?: number;  vat_total?: number; total?: number; notes?: string;  purchase_order_lines?: Array<{ description?: string; qty?: number; unit_price?: number; total?: number }>;};export default function PurchaseOrderDetailPage() {  const router = useRouter();  const params = useParams();  const [loading, setLoading] = useState(true);  const [po, setPo] = useState<PODetail | null>(null);  useEffect(() => {    fetch(`/api/purchase-orders?id=${params.id}`)      .then((r) => r.json())      .then((data) => { setPo(data); setLoading(false); })      .catch(() => setLoading(false));  }, [params.id]);  if (loading) return <p className="text-center py-8 text-[#64748b]">جار التحميل...</p>;  if (!po) return <p className="text-center py-8 text-[#dc2626]">لم يتم العثور على أمر الشراء</p>;  return (    <div>      <PageHeader title={`أمر شراء #${po.number}`} description={`تاريخ: ${po.date}`}        action={<Button variant="outline" onClick={() => router.back()}>رجوع</Button>} />      <Card><CardContent className="p-6">        <div className="grid gap-4 sm:grid-cols-3 mb-4">          <div><Label>الرقم</Label><p className="font-medium">{po.number}</p></div>          <div><Label>التاريخ</Label><p className="font-medium">{po.date}</p></div>          <div><Label>الحالة</Label><p className="font-medium">{po.status}</p></div>        </div>        <div className="overflow-x-auto mb-4">          <table className="w-full text-sm">            <thead><tr className="border-b bg-[#f8fafc]">              <th className="px-4 py-2 text-right">الصنف</th><th className="px-4 py-2 text-right">الكمية</th>              <th className="px-4 py-2 text-right">سعر الوحدة</th><th className="px-4 py-2 text-right">الإجمالي</th>            </tr></thead>            <tbody>              {(po.purchase_order_lines || []).map((l, i) => (                <tr key={i} className="border-b">                  <td className="px-4 py-2">{l.description}</td>                  <td className="px-4 py-2">{l.qty}</td>                  <td className="px-4 py-2">{l.unit_price?.toLocaleString()} ر.س</td>                  <td className="px-4 py-2">{l.total?.toLocaleString()} ر.س</td>                </tr>              ))}            </tbody>          </table>        </div>        <div className="text-left space-y-1">          <p className="text-sm">المجموع الفرعي: {po.subtotal?.toLocaleString()} ر.س</p>          <p className="text-sm">الضريبة: {po.vat_total?.toLocaleString()} ر.س</p>          <p className="text-lg font-bold">الإجمالي: {po.total?.toLocaleString()} ر.س</p>        </div>        {po.notes && <p className="mt-4 text-sm text-[#64748b]">{po.notes}</p>}      </CardContent></Card>    </div>  );}
